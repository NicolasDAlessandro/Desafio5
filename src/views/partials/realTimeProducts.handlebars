    <div id="products">
        {{#each Products}}
        <div>
            <br>
            <p>{{this.title}}</p>
            <p>{{this.price}}</p>
            <p>{{this.description}}</p>
            <br>
        </div>    
        {{/each}}
    </div>
